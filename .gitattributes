# 统一换行符：仓库内始终存 LF，检出时由 git 处理
* text=auto

# Shell 脚本必须是 LF，否则在 Linux/CI 下无法执行
mvnw text eol=lf
*.sh text eol=lf

# Windows 批处理保持 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.jar binary
